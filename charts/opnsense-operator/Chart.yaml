apiVersion: v2
name: opnsense-operator
description: Kubernetes operator that programs OPNsense HAProxy (and Unbound DNS / ACME) from Service annotations
type: application
# version (chart) and appVersion (image tag) are overwritten from the git tag at
# release time; these are dev defaults.
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.27.0-0"
home: https://github.com/trickyearlobe/opnsense-operator
sources:
  - https://github.com/trickyearlobe/opnsense-operator
maintainers:
  - name: trickyearlobe
keywords:
  - opnsense
  - haproxy
  - loadbalancer
  - operator
  - cilium
