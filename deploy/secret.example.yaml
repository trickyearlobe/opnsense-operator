# Example only — DO NOT COMMIT real credentials.
# Prefer: kubectl create secret ... (see deployment.yaml) or an ExternalSecret.
apiVersion: v1
kind: Secret
metadata:
  name: opnsense-api
  namespace: opnsense-system
type: Opaque
stringData:
  url: https://opnsense.lan
  key: REPLACE_WITH_API_KEY
  secret: REPLACE_WITH_API_SECRET
